#include <stdio.h>

int main() {
  const int toes = 10;
  int twice_toes = 2 * toes;
  int toes_squared = toes * toes;

  printf("toes: %d\n", toes);
  printf("twice toes: %d\n", twice_toes);
  printf("toes squared: %d\n", toes_squared);

  return 0;
}